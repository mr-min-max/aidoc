# Unpublished v0.2.0-beta.3 candidate

This note records the source-checkout beta.3 slice and its failed publication
attempt. The release workflow verified the exact artifact and generated
provenance, but npm rejected the unscoped package name because it was too
similar to an existing package. npm did not create a package version and the
workflow did not create a GitHub Release. The public tag is preserved and will
not be moved, deleted, rerun, or reused. The scoped beta.4 candidate supersedes
it.

## Included

- Truthful `working-tree` labels in verbose human plan output without changing
  the versioned JSON descriptor.
- Pre-Git rejection of every C0 control character and DEL in revision inputs.
- A repository-owned Codex plugin at `integrations/codex/aidoc`.
- The `maintain-documentation` skill with a fail-closed
  `prepare_documentation_update` → host generation →
  `validate_documentation_draft` workflow.
- A local MCP server mapping that invokes `aidoc --mcp` with no embedded
  credentials or host OAuth access.
- Credential-free, offline hybrid-beta demo evidence for no-impact planning,
  target selection, provider-free MCP validation, Trust Gate safety, and plugin
  smoke coverage.
- Truthful Codex and Claude Desktop/Code integration documentation.

## Access paths

An eligible ChatGPT subscription authenticates the official local Codex host;
it does not become an OpenAI API key and AiDoc receives no ChatGPT OAuth token.
Claude Desktop and Claude Code authenticate their own host sessions; AiDoc
receives no Claude subscription token. Consumer subscriptions and API billing
are separate.

Direct AiDoc provider mode supports exactly `openai`, `anthropic`, `deepseek`,
`qwen`, `openai-compatible`, and `ollama`. Remote credential variables are
`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `DEEPSEEK_API_KEY`,
`DASHSCOPE_API_KEY`, and `AIDOC_COMPAT_API_KEY`. Ollama is local and requires
an explicit model. When interactive selection has no configured Ollama model,
AiDoc discovers installed models through the approved loopback `/api/tags`
endpoint and asks the user to select one; non-interactive runs remain explicit.
Direct provider mode never silently falls back. Qwen custom AiDoc calls are
PAYG Model Studio API use, not a consumer-subscription bridge.

## Source-checkout verification

```bash
npm install
npm run build
npm link
aidoc --version
```

Reverse with:

```bash
npm unlink -g aidoc-gen
```

The plugin validator, semantic plugin smoke, offline demo, focused beta docs
assertions, and public-beta preflight are the local evidence for this slice.
No installation to a marketplace or publication to npm is performed here.

## Pinned MCP repository scope

Each MCP server is pinned to the canonical Git worktree containing its startup
cwd. One MCP server serves one repository; to work on another repository,
start another server from that repository. The root and real subdirectories
are allowed, including absolute in-worktree paths and repository-relative
directory paths.

External paths, parent traversal, `.git` or other Git metadata, missing or
non-directory paths, and every symlink or junction path fail closed before
project reads. Successful MCP paths are repository-relative POSIX paths.

MCP configuration is bounded from the selected directory up to the pinned
root. It accepts declarative JSON/YAML/no-extension configuration,
`package.json#aidoc`, and the bounded root `.env` allowlist. MCP rejects
malformed or symlinked selected configuration, executable JavaScript,
TypeScript, CJS, or MJS configuration, and the legacy secret-bearing `apiKey`
field. Direct CLI cosmiconfig and dotenv behavior remains unchanged.

This is a repository path/read boundary, not an operating-system sandbox. A
privileged same-host process can race entries between checks, hard links are
indistinguishable from ordinary repository files at this API level, and
network access remains controlled by the selected provider transport and Trust
Gate.

## Boundary and limitations

AiDoc Trust Gate inspects AiDoc's prepared input and validated output.
Configured `strict` blocks findings; configured `warn` or `redact` redacts
detected sensitive values before host generation or return. An `allowed` result
means no findings were detected. Trust Gate does not control a host's context
window, model, sandbox, isolation, or permission system. The host's normal
permissions remain in force. The MCP prepare/validate workflow never writes
the repository, and validation failures are fail-closed: the host stops or
prepares again rather than bypassing the digest or target checks.
