# StaleDocs 0.4.0-beta.1

Status: Release candidate, not published

## What changed for users

- TypeScript and JavaScript public APIs are resolved from package entries and bounded static re-exports, with explicit fallback when no supported entry is available.
- Entry-only changes can expose or hide existing symbols. Python boundaries use package roots, literal `__all__`, relative imports, and private underscore paths.
- Review reports fold duplicate member rows, avoid false unmentioned-class rows, and do not treat changelog history as stale prose.
- Non-enumerable CommonJS and unsupported files are reported as not analyzed rather than silently presented as clean.
- `comment: on-findings` keeps Action comments to stale or breaking verdicts.
- Markdown discovery covers root documents, common documentation directories, package-local Markdown, configured output, and safe `docs` paths with bounded deterministic scans.
- `entry` and `docs` configuration can specify repository-relative paths. Recommendations prefer the nearest README.

The plan, context, and review schemas keep their existing v1 identities. Boundary reports, visibility, internal-change counts, not-analyzed details, and documentation-limit reporting are optional additive fields.

## Evaluation proof sequence

PR A records ten owner-approved labels before running either package in this phase: seven TypeScript targets and three Python targets, including two INTERNAL cases, two NO-PUBLIC-CHANGE controls, and one DOCS-STALE case. Quota-driven skips and a documentation-only control were approved by the owner before execution; reasons are recorded privately.

The evaluator accepts an exact published package through `--package`, validates expected labels, and compares saved evidence with `--compare`. Observations come from review JSON counts. Error rows stay in the corpus, and a decrease in matched expectations for any label class stops comparison without changing labels.

The old-package evidence uses the real published `staledocs@0.3.0-beta.1`. The new-package run and regenerated `docs/EVALUATIONS.md` belong to PR B after `0.4.0-beta.1` is published. No before/after result for the unpublished package is claimed here.

## Remaining owner gates

- Supply the private live Codex fix-path record: date, Codex version, preparation payload size, first-candidate validation result, diff, and manual correctness assessment. A sanitized recorded failure is acceptable; an unrun success claim is not.
- Merge PR A after verification, repeat release checks at the pinned clean main commit, and confirm Private vulnerability reporting is enabled.
- Make a separate publication decision, push the annotated `v0.4.0-beta.1` tag, and verify the OIDC workflow and checksum-matching npm/GitHub prerelease artifacts.
- Move npm `latest` and `beta`, move the existing `v0` tag, and update Marketplace only if required.
- Start PR B only after the real package is published.

See [RELEASING.md](../RELEASING.md) for the procedure. Publication uses the existing npm Trusted Publisher; no npm name claim, placeholder, reusable credential, or Marketplace change is part of PR A.
