# v0.2.0-beta.2 — Source Beta Candidate

`0.2.0-beta.2` is the current source-beta candidate. It has not been tagged,
published to npm, or released on GitHub.

## Highlights

- provider-free `aidoc plan` with human and versioned JSON output;
- TypeScript/JavaScript/Python AST snapshots and semantic impact categories;
- deterministic documentation mapping and exact UTF-8 context budgets;
- shared planning core across CLI, MCP, and provider-backed updates;
- Provider Trust Gate with fixed, value-safe diagnostics;
- release-integrity checks for the packed CLI, Action, and MCP server.

## Verification checkpoint

At commit `27b334e`, the candidate passed:

- 46/46 Jest suites and 452/452 tests;
- Node 22 and Node 24 hosted CI;
- TypeScript build and ESLint;
- provider-free impact demo and CLI human/JSON smoke;
- installed-tarball package smoke, Action tests, and MCP smoke;
- documentation score 81 with no low-quality findings.

Public-beta preparation adds onboarding and privacy checks on top of that
checkpoint; final counts and the exact candidate commit will be recorded in
pull request #2 before integration.

## Known limits

See [the Public Beta guide](../PUBLIC_BETA.md#beta-boundaries). These limits are
non-blocking for source testing and must remain visible until fixed.

## Release state

- npm package: not published
- Git tag: not created
- GitHub Release: not created
- canonical repository: private pending publication clearance
