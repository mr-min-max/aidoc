# v0.2.0-beta.6

Status: Forthcoming candidate

This source checkout carries a local `0.2.0-beta.6` candidate for later Gate A
review. It is not a registry artifact or a public install.

Candidate evidence groups:

- aligned AST-first storefront copy;
- deterministic `createUser` provider-free demo;
- original logo, poster, social preview, and short GIF;
- progressive CLI and Action documentation;
- No runtime, provider, MCP, security, or model change;
- intended OIDC-only beta publication;
- `latest` remains `0.2.0-beta.4`.

`0.2.0-beta.5` remains the current public beta on npm's `beta` channel. Keep
public installation at `npm install -g @mr-min-max/aidoc-gen@beta` and public
GitHub Action examples at `v0.2.0-beta.5` until a separate publication
decision and live release evidence exist. The candidate is not tagged,
published, or installable from npm by this change.
