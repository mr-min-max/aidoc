# v0.2.0-beta.6

Status: Published public beta

`@mr-min-max/aidoc-gen@0.2.0-beta.6` is published on npm and available through
the explicit beta install path:

```bash
npm install -g @mr-min-max/aidoc-gen@beta
```

Release contents:

- aligned AST-first storefront copy;
- deterministic `createUser` provider-free demo;
- original logo, poster, social preview, and short GIF;
- progressive CLI and Action documentation;
- No runtime, provider, MCP, security, or model change from beta.5.

Publication evidence:

- npm Trusted Publishing (OIDC) published from
  `https://github.com/mr-min-max/aidoc` through `.github/workflows/release.yml`;
- the [release workflow](https://github.com/mr-min-max/aidoc/actions/runs/31914951538)
  completed successfully on hosted Node 22 and 24 verification jobs;
- the [GitHub prerelease](https://github.com/mr-min-max/aidoc/releases/tag/v0.2.0-beta.6)
  contains the verified tarball and checksum;
- the registry tarball SHA-256 is
  `b68ec3a879d5e84837bb98a868de9741d2c8a840c019f8f7a560396e998372c1`,
  matching the GitHub checksum asset;
- a clean registry installation reports `0.2.0-beta.6`, and the exact
  registry bytes pass the packaged CLI and MCP smoke tests;
- npm `beta` resolves to `0.2.0-beta.6`; `latest` remains `0.2.0-beta.4`.

The protected annotated tag resolves to main commit
`fd58309943a161aadce812bda43c60722abbf972`. npm provenance names the
`mr-min-max/aidoc` repository, `release.yml`, and
`refs/tags/v0.2.0-beta.6`.
